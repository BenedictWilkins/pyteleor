from setuptools import setup

setup(
   name='pyteleor',
   version='0.0.1',
   description='Teleo-reactive agents in Python.',
   author='Benedict Wilkins',
   author_email='benrjw@googlemail.com',
   packages=['pyteleor'],  #same as name
   install_requires=[], #external packages as dependencies
)